pipeline{
    agent{
        dockerfile {
            filename 'src/Dockerfile'
        }
    }
    stages{
        stage('Build'){
            steps{
                sh "cd src/app"
                sh "node --version"
                sh "npm install"
                sh "ng build -configuration production"
            }
        }
    }
}