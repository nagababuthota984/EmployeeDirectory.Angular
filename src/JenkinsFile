pipeline{
    agent{
        docker {
            image 'empdir-angular:0.3'
        }
    }
    tools {
        nodejs "nodejs"
    }
    stages{
        stage('Build'){
            steps{
                sh 'cd src/app && npm install && ng build -configuration production'
            }
        }
    }
}