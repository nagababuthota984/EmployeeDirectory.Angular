pipeline{
    agent{
        docker {
            dockerfile : true
        }
    }
    stages{
        stage('Build'){
            steps{
                sh "cd src/app"
                sh "node --version"
                sh "npm install"
                sh "ng build -configuration production"
            }
        }
    }
}